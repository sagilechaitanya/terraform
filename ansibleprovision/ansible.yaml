---
pool: 'default'
trigger:
  - main
stages:
- stage: terraformcalling
  jobs:
  - job: callingterraform
    steps: 
    - script: sh "terraform init"
    - script: sh "terraform fmt"
    - script: sh "terraform terraform validate"
    - script : sh "terraform aplly -auto-approve"